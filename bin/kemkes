#!/usr/bin/env bash

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
CWD="$(pwd)"
BUILD_SCRIPT="$SCRIPT_DIR/build.mjs"

echo "Running build script in $CWD"
echo "Build script path: $BUILD_SCRIPT"

node "$BUILD_SCRIPT"

node "$CWD/dist/runner/sehatindonesiaku-kemkes.js" "$@"
